<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MoistFragment">

    <ProgressBar
        android:id="@+id/moistProgressBar"
        android:layout_width="290dp"
        android:layout_height="290dp"
        android:indeterminateOnly="false"
        android:progressDrawable="@drawable/guage_bg"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        />//android:progress="40"

    <TextView
        android:layout_width="wrap_content"
        android:id="@+id/moistProgressBarText"
        android:layout_height="wrap_content"
        android:textColor="@color/subWhite"
        app:layout_constraintStart_toStartOf="@id/moistProgressBar"
        app:layout_constraintEnd_toEndOf="@id/moistProgressBar"
        app:layout_constraintTop_toTopOf="@id/moistProgressBar"
        app:layout_constraintBottom_toBottomOf="@id/moistProgressBar"
        android:textSize="40dp"
         /> //android:text="40%"

</androidx.constraintlayout.widget.ConstraintLayout>